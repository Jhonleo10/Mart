export {
  DashboardFilterBar as AdminFilterBar,
  type DashboardFilterField,
} from "@/components/dashboard/dashboard-filter-bar";

/** Admin tables use the compact filter row by default. */
export { DashboardFilterBar as AdminCompactFilterBar } from "@/components/dashboard/dashboard-filter-bar";
